---
phase: 01-cardswipescreen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/core/theme/app_theme.dart
autonomous: true
requirements:
  - DISC-04

must_haves:
  truths:
    - "flutter_card_swiper ^7.0.0 is a declared dependency"
    - "skeletonizer ^1.x is a declared dependency"
    - "flutter pub get resolves cleanly with no version conflicts"
    - "AppTheme.dark configures SkeletonizerConfigData.dark() for the shimmer theme"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Dependency declarations"
      contains: "flutter_card_swiper"
    - path: "lib/core/theme/app_theme.dart"
      provides: "Skeletonizer theme configuration"
      contains: "SkeletonizerConfigData"
  key_links:
    - from: "pubspec.yaml"
      to: "flutter pub get"
      via: "dart pub resolve"
      pattern: "flutter_card_swiper: \\^7\\.0\\.0"
    - from: "lib/core/theme/app_theme.dart"
      to: "Skeletonizer widget"
      via: "SkeletonizerConfigData extension on ThemeData"
      pattern: "SkeletonizerConfigData\\.dark"
---

<objective>
Add `flutter_card_swiper ^7.0.0` and `skeletonizer ^1.x` to `pubspec.yaml`, run `flutter pub get` to resolve, and configure `SkeletonizerConfigData.dark()` inside `AppTheme.dark` so the shimmer matches the app's dark theme.

Purpose: Plans 02 and 03 depend on these packages being available. `SkeletonizerConfigData` must be in `AppTheme` so the shimmer picks up dark colours globally rather than requiring per-widget configuration.

Output: Updated `pubspec.yaml` with both packages; updated `app_theme.dart` with skeletonizer theme extension; clean `flutter pub get` output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flutter_card_swiper and skeletonizer to pubspec.yaml</name>
  <files>pubspec.yaml</files>
  <read_first>
    - pubspec.yaml — read current dependency list before editing to avoid duplicates or version conflicts
  </read_first>
  <action>
Add two new entries to the `dependencies:` section of `pubspec.yaml` (after `hive_ce_flutter`):

```yaml
flutter_card_swiper: ^7.0.0
skeletonizer: ^1.4.0
```

Then run: `flutter pub get`

The command must exit 0 with no version-conflict errors. `cached_network_image: ^3.4.1` is already present — do NOT add it again.
  </action>
  <verify>
    <automated>grep -n "flutter_card_swiper\|skeletonizer" /Users/simonreich/git/private/random-magic/pubspec.yaml</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains the line: `flutter_card_swiper: ^7.0.0`
    - pubspec.yaml contains the line: `skeletonizer: ^1.4.0`
    - `flutter pub get` exits with code 0 (no resolution errors)
  </acceptance_criteria>
  <done>Both packages declared in pubspec.yaml; flutter pub get resolves without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Configure SkeletonizerConfigData.dark() in AppTheme</name>
  <files>lib/core/theme/app_theme.dart</files>
  <read_first>
    - lib/core/theme/app_theme.dart — read the existing ThemeData.dark builder; the new extension must be added inside the `ThemeData(...)` call using `extensions: [...]`
  </read_first>
  <action>
Add `skeletonizer` import at the top of `app_theme.dart`:

```dart
import 'package:skeletonizer/skeletonizer.dart';
```

Inside `AppTheme.dark` → `ThemeData(...)` constructor, add an `extensions` parameter:

```dart
extensions: const [
  SkeletonizerConfigData.dark(),
],
```

Place it after the existing `elevatedButtonTheme:` entry. This registers the dark shimmer palette globally so every `Skeletonizer` widget in the app automatically uses dark shimmer colours without requiring explicit `SkeletonizerConfigData` on each call site.

No other changes. Do NOT alter `AppColors` or any existing theme property.
  </action>
  <verify>
    <automated>grep -n "SkeletonizerConfigData\|skeletonizer" /Users/simonreich/git/private/random-magic/lib/core/theme/app_theme.dart</automated>
  </verify>
  <acceptance_criteria>
    - app_theme.dart contains: `import 'package:skeletonizer/skeletonizer.dart';`
    - app_theme.dart contains: `SkeletonizerConfigData.dark()`
    - `flutter analyze lib/core/theme/app_theme.dart` exits 0 with no errors or warnings
  </acceptance_criteria>
  <done>AppTheme.dark registers SkeletonizerConfigData.dark() as a theme extension; flutter analyze is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev → pubspec.yaml | Third-party package declarations; version pinning prevents supply-chain drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | pubspec.yaml dependency versions | mitigate | Pin with caret constraints (`^7.0.0`, `^1.4.0`) to allow only patch-level updates; lock via `pubspec.lock` committed to git |
| T-01-02 | Spoofing | flutter_card_swiper package identity | accept | Package is well-known on pub.dev; no auth token or PII involved; low-value target |
</threat_model>

<verification>
After both tasks complete:

```bash
flutter pub get
flutter analyze lib/core/theme/app_theme.dart
```

Both must exit 0. Confirm pubspec.yaml contains both new package lines.
</verification>

<success_criteria>
- `pubspec.yaml` declares `flutter_card_swiper: ^7.0.0` and `skeletonizer: ^1.4.0`
- `flutter pub get` resolves with no errors
- `AppTheme.dark` includes `extensions: const [SkeletonizerConfigData.dark()]`
- `flutter analyze lib/core/theme/app_theme.dart` is clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-cardswipescreen/01-01-SUMMARY.md`
</output>
