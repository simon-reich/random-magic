---
phase: 01-cardswipescreen
plan: 02
subsystem: api
tags: [flutter, riverpod, scryfall, failures, sealed-class, riverpod-annotation]

# Dependency graph
requires: []
provides:
  - RateLimitedFailure typed failure in AppFailure sealed class hierarchy
  - Defensive legalities map parsing in MagicCard.fromJson()
  - HTTP 429 mapped to RateLimitedFailure in ScryfallApiClient
  - activeFilterQueryProvider null stub in filters/presentation/providers.dart
  - RandomCardNotifier marked keepAlive: true wired to activeFilterQueryProvider
affects:
  - 01-03-cardswipescreen (requires RateLimitedFailure for exhaustive switch; requires activeFilterQueryProvider import path)
  - phase-02-filters (replaces activeFilterQuery stub with real filter state)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sealed class AppFailure extended with RateLimitedFailure for HTTP 429 typed error propagation"
    - "Defensive Map parsing: rawMap.map((k, v) => MapEntry(k.toString(), v.toString())) avoids dynamic cast errors"
    - "@Riverpod(keepAlive: true) on cross-tab notifiers to survive StatefulShellRoute tab switches"
    - "activeFilterQueryProvider null stub pattern for phase-gated feature enablement"

key-files:
  created:
    - lib/features/filters/presentation/providers.dart
    - lib/features/filters/presentation/providers.g.dart
  modified:
    - lib/shared/failures.dart
    - lib/shared/models/magic_card.dart
    - lib/features/card_discovery/data/scryfall_api_client.dart
    - lib/features/card_discovery/presentation/providers.dart
    - lib/features/card_discovery/presentation/providers.g.dart

key-decisions:
  - "activeFilterQueryProvider created in filters/presentation (not shared/) to maintain feature isolation; card_discovery imports it as a cross-presentation import which is acceptable for providers"
  - "RandomCardNotifier.build() watches activeFilterQueryProvider so filter changes auto-rebuild the card state in Phase 2 without further provider changes"

patterns-established:
  - "Phase stub pattern: create provider returning null/empty now; Phase N replaces implementation"
  - "keepAlive on notifiers that hold UI state across bottom-tab navigation"

requirements-completed:
  - QA-05
  - QA-06

# Metrics
duration: 8min
completed: 2026-04-12
---

# Phase 01 Plan 02: Domain Layer Fixes and Provider Scaffolding Summary

**RateLimitedFailure added to AppFailure sealed class, legalities map parsing hardened, and activeFilterQueryProvider null stub wired into keepAlive RandomCardNotifier**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-12T07:38:00Z
- **Completed:** 2026-04-12T07:46:08Z
- **Tasks:** 2
- **Files modified:** 7 (5 modified, 2 created)

## Accomplishments

- Extended `AppFailure` sealed class with `RateLimitedFailure` for HTTP 429 — Plan 03 can now exhaustively pattern-match all failure types
- Replaced `rawLegalities.cast<String, String>()` with `.map((k, v) => MapEntry(k.toString(), v.toString()))` — eliminates `Map<dynamic, dynamic>` cast errors on Scryfall production data
- Created `activeFilterQueryProvider` null stub in filters feature; `RandomCardNotifier` is now `keepAlive: true` and rebuilds automatically when filters change in Phase 2

## Task Commits

Each task was committed atomically:

1. **Task 1: Add RateLimitedFailure and fix legalities parsing** - `2b84f0a` (feat)
2. **Task 2: Scaffold activeFilterQueryProvider stub and mark RandomCardNotifier keepAlive** - `086d0d1` (feat)

## Files Created/Modified

- `lib/shared/failures.dart` - Added `RateLimitedFailure final class` after `NetworkFailure`
- `lib/shared/models/magic_card.dart` - Fixed legalities line to use `.map((k, v) => MapEntry(k.toString(), v.toString()))`
- `lib/features/card_discovery/data/scryfall_api_client.dart` - Added `if (statusCode == 429) return const RateLimitedFailure();` in `_mapDioException()`
- `lib/features/filters/presentation/providers.dart` - Created with `activeFilterQuery` provider returning `null`
- `lib/features/filters/presentation/providers.g.dart` - Generated by build_runner
- `lib/features/card_discovery/presentation/providers.dart` - Changed `@riverpod` to `@Riverpod(keepAlive: true)` on `RandomCardNotifier`; added `ref.watch(activeFilterQueryProvider)` in `build()`
- `lib/features/card_discovery/presentation/providers.g.dart` - Regenerated by build_runner

## Decisions Made

- `activeFilterQueryProvider` placed in `lib/features/filters/presentation/providers.dart` (not `lib/shared/`) to maintain feature ownership — filters feature owns filter state. Card discovery imports it directly, which is acceptable for providers (not data/domain layers).
- `RandomCardNotifier.build()` watches the provider directly so Phase 2 filter changes cause automatic card refresh with zero additional wiring.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `RateLimitedFailure` is in the sealed hierarchy — Plan 03 `CardSwipeScreen` can use exhaustive `switch` on all 4 failure types
- `activeFilterQueryProvider` is importable from `lib/features/filters/presentation/providers.dart`
- `RandomCardNotifier` survives tab navigation and will auto-rebuild when Phase 2 wires real filter state

---
*Phase: 01-cardswipescreen*
*Completed: 2026-04-12*
