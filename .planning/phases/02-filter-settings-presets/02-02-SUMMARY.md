---
plan: "02-02"
phase: "02-filter-settings-presets"
status: completed
wave: 2
completed_at: "2026-04-12"
---

# Plan 02-02 Summary — Business Logic Layer

## What Was Built

Implemented the full business logic layer for Phase 2: `ScryfallQueryBuilder` (pure static query builder), `FilterPresetRepository` (Hive CE CRUD wrapper), and replaced the Phase 1 stub in `providers.dart` with three production providers.

## Key Files

### Created
- `lib/features/filters/data/scryfall_query_builder.dart` — `abstract final class ScryfallQueryBuilder` with `fromSettings()` static method. Returns null for empty settings (FILT-10), builds color:/type:/rarity:/date clauses with OR-joined paren groups.
- `lib/features/filters/data/filter_preset_repository.dart` — `FilterPresetRepository` with `Result<void> save()`, `void delete()`, `List<FilterPreset> getAll()`. Duplicate-name guard via `DuplicatePresetNameFailure` (FILT-09).

### Modified
- `lib/shared/failures.dart` — added `DuplicatePresetNameFailure` sealed subclass (FILT-09)
- `lib/features/filters/presentation/providers.dart` — replaced stub with `FilterSettingsNotifier` (live filter state + D-12 `activePresetName`), `activeFilterQuery` (computed query, `activeFilterQueryProvider` name preserved), `FilterPresetsNotifier` (Hive CRUD)
- `lib/features/filters/presentation/providers.g.dart` — regenerated by build_runner (Riverpod 3.x naming: `filterSettingsProvider`, `filterPresetsProvider`, `activeFilterQueryProvider`)
- `test/unit/filters/scryfall_query_builder_test.dart` — Wave 0 stubs replaced with 12 real assertions
- `test/unit/filters/filter_settings_notifier_test.dart` — Wave 0 stubs replaced with 19 real assertions including D-12 dirty-state tracking
- `test/unit/filters/filter_presets_notifier_test.dart` — Wave 0 stubs replaced with 6 real assertions

## Commits
- `48b5fb0` feat(02-02): implement ScryfallQueryBuilder and FilterPresetRepository
- `5d4c258` feat(02-02): replace providers stub with FilterSettingsNotifier, activeFilterQuery, FilterPresetsNotifier

## Self-Check

### must_haves verification
- [x] `ScryfallQueryBuilder.fromSettings()` returns null for empty FilterSettings (FILT-10)
- [x] `ScryfallQueryBuilder.fromSettings()` builds correct color:/type:/rarity:/date queries
- [x] `activeFilterQueryProvider` name preserved — RandomCardNotifier still compiles
- [x] `FilterSettingsNotifier` holds FilterSettings state with all 7 mutation methods
- [x] `FilterSettingsNotifier.activePresetName` is set by loadPreset() and cleared by all mutation methods (D-12)
- [x] `FilterPresetsNotifier.save()` blocks duplicate names and returns false (FILT-09)
- [x] `FilterPresetsNotifier.save(upsert: true)` replaces existing preset
- [x] `FilterPresetsNotifier.delete()` removes preset from Hive box
- [x] `flutter analyze --fatal-infos` passes clean after build_runner

### Test results: 37/37 passing

### Self-Check: PASSED
