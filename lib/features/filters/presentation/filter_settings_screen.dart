import 'package:flutter/material.dart';

/// Placeholder for the filter settings screen.
///
/// Will be replaced with the full filter implementation in a later ticket.
class FilterSettingsScreen extends StatelessWidget {
  const FilterSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Filter Settings'),
      ),
    );
  }
}
