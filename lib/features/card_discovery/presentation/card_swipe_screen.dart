import 'package:flutter/material.dart';

/// Placeholder for the main card discovery / swipe screen.
///
/// Will be replaced with the full swipe implementation in a later ticket.
class CardSwipeScreen extends StatelessWidget {
  const CardSwipeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Card Discovery'),
      ),
    );
  }
}
